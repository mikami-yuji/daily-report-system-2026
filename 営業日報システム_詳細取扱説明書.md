# 営業日報システム (Daily Report System) 詳細取扱説明書

本マニュアルは、エクセルベースの営業日報をモダンなWebインターフェースで効率的に管理・分析するための「営業日報システム」の導入・操作・保守に関する詳細な取扱説明書です。

---

## 🧭 目次

1. [システム概要と特徴](#1-システム概要と特徴)
2. [動作環境と事前準備](#2-動作環境と事前準備)
3. [システム起動・セットアップ方法](#3-システム起動セットアップ方法)
4. [各画面の構成と操作マニュアル](#4-各画面の構成と操作マニュアル)
5. [Excel連携およびデータ管理の仕様](#5-excel連携およびデータ管理の仕様)
6. [トラブルシューティング](#6-トラブルシューティング)
7. [管理者・保守担当者向け情報](#7-管理者保守担当者向け情報)

---

## 1. システム概要と特徴

本システムは、従来のエクセル（`.xlsm`）による日報管理の良さ（関数やマクロ、ネットワーク共有による管理）をそのまま活かしつつ、入力作業のストレスを軽減し、データの検索・分析能力を大幅に強化したハイブリッド型業務システムです。

```mermaid
graph TD
    User([ユーザー]) <-->|ブラウザ (モダンWeb UI)| Frontend[フロントエンド画面]
    Frontend <-->|API通信 (JSON)| BackendExe[バックエンド実行ファイル: DailyReportServer.exe]
    BackendExe <-->|openpyxl / pandas| ExcelFile[(共有エクセルファイル .xlsm)]
    BackendExe -->|自動実行| Backup[(バックアップ .bak)]
```

### ✨ 主な特徴とメリット
* **既存エクセルの完全再利用**: エクセル内に書き込まれた関数やマクロ、書式を壊すことなく、Web画面から直接データの読み書きを行います。
* **直感的で洗練されたUI**: 営業担当者がスマホやPCからストレスなく迅速に入力できるよう、Salesforce風のモダンで美しいカード型デザインを採用しています。
* **オフライン対応と自動同期**: 移動中や圏外でも日報の下書き入力ができ、オンライン時にワンクリックでエクセルへ反映・自動同期できます。
* **高度な検索と見える化**: 大量の日報データから、得意先やデザイン種別によるクロス検索、活動量グラフ、メンバーの状況比較などをビジュアルに分析可能です。
* **安心のデータ保護**: 保存時に自動的にバックアップ（`.bak`）を生成するため、不意のフリーズやネットワーク瞬断によるデータ破損を防ぎます。

---

## 2. 動作環境と事前準備

本システムを利用するにあたり、以下の環境および準備が必要です。

### 💻 クライアントPC環境
* **OS**: Windows 10 / 11 (64bit)
* **ブラウザ**: モダンなWebブラウザ (Google Chrome, Microsoft Edge 推奨)
* **ネットワーク**: 社内LAN環境（または社外からのVPN接続環境）
  * 共有フォルダ上のエクセルファイル（`\\Asahipack02` 等）に直接読み書きを行うため、対象の共有サーバーにエクスプローラー等からアクセスできる状態である必要があります。

---

## 3. システム起動・セットアップ方法

本システムは、複雑な環境構築（PythonやNode.jsのインストールなど）を行うことなく、パッケージ化されたEXEファイルをダブルクリックするだけで簡単に起動できます。

### 🚀 通常の起動方法（推奨）
1. システムフォルダ内にある **`DailyReportServer.exe`** をダブルクリックして実行します。
2. 黒いコマンドプロンプト画面（サーバー画面）が立ち上がり、自動的にシステムサーバーが起動します。
3. ブラウザ（Google Chrome または Microsoft Edge 推奨）を開き、以下のURLへアクセスします。
   * **URL**: `http://localhost:8001` （デフォルト設定の場合）
4. 画面が表示されたらシステムが使用可能な状態になります。
   * *※サーバーが起動している間は、立ち上がった黒い画面を閉じないでください（最小化しておくことを推奨します）。終了する際は、黒い画面の「×」ボタンを押して閉じてください。*
   * *※また、パッケージ内に同梱されている `営業日報システム起動.vbs` を使用すると、黒い画面を非表示の状態で起動し、自動的にブラウザを開くことも可能です。*

### 🏢 会社PCでの初回セットアップ方法
社内共有のネットワークフォルダを利用して複数名で日報を管理する場合、以下の設定を実行してください。
1. `DailyReportServer.exe` と同じフォルダ内にある **`config.json`** をメモ帳などのテキストエディタで開きます。
2. `"excel_dir"` の値を、社内共有フォルダ上の日報エクセルファイルが保存されているディレクトリのパス（絶対パス）に書き換えます。
   * 例: `"excel_dir": "\\\\Asahipack02\\Shared\\営業部\\日報\\2026年度"`
   * *※バックスラッシュ（`\`）は必ず二重（`\\`）で記述してください。*
3. `config.json` を保存して閉じ、改めて `DailyReportServer.exe` を起動します。

### 🛠️ デバッグ・手動での個別起動手順
バッチや自動起動スクリプトが動作しない場合は、以下のコマンドで個別に起動できます。

1. **バックエンドサーバーの起動**:
   ```powershell
   cd backend
   python main.py
   ```
2. **フロントエンドサーバーの起動** (別のターミナルを開く):
   ```powershell
   cd frontend
   npm run dev
   ```

---

## 4. 各画面の構成と操作マニュアル

本システムは、左側のナビゲーションメニュー（サイドバー）から各機能へ簡単にアクセスできます。サイドバーが縮小（折りたたみ）されている場合は、上部の三本線（メニュー）アイコンを押すことで開閉可能です。

---

### 📊 ① ホーム（ダッシュボード）
起動後に最初に表示される、全体の活動状況の「見える化」と「エクセルファイル管理」を行う画面です。

#### 🔹 主な機能と活用方法:
1. **アクティブファイルの切り替え**:
   * 現在システムが読み書きしているエクセルファイル名（例：`日報_2026.xlsm`）が上部に表示されます。
   * 別のエクセルに切り替えるには、ファイルを画面の枠内に直接ドラッグ＆ドロップするか、「ファイルをアップロード」ボタンをクリックしてアップロードします。
2. **活動サマリーカード**:
   * 今月の「訪問件数」および「電話連絡数」の合計値がカード形式で強調表示されます。
3. **月次活動実績推移グラフ**:
   * 月別の訪問数・電話連絡数の推移がビジュアルグラフで描画され、営業活動の波やアプローチ実績を一目で把握できます。

---

### 📋 ② 日報一覧
過去に登録された日報を、一覧表形式で閲覧・検索する画面です。

#### 🔹 主な機能と活用方法:
* **複合フィルター検索**:
  * 右上の検索フィルターを利用し、日付や担当者名、得意先名で瞬時に絞り込みが可能です。
* **商談内容のポップアップ表示**:
  * 各行をクリックすると、その日に登録された具体的な商談内容や詳しい状況のポップアップが開き、その場で詳細を確認できます。

---

### 📝 ③ 新規日報入力
エクセルを開くことなく、直感的なWebフォームから日報を直接エクセルに追加できるメインの入力画面です。

#### 💡 入力をサポートするスマート動的フォーム:
本画面は、選択した活動内容に合わせて、入力に必要な項目のみが動的に変化する「スマート入力フォーム」を採用しています。

1. **行動内容によるフォームの動的切り替え**:
   * **「社内作業」「量販店調査」などを選択した場合**:
     * 訪問先名、面談者、デザイン仕様などの営業用入力項目が**自動的に非表示**になり、無駄な入力を省きます。
   * **「外出時間」を選択した場合**:
     * 専用項目である**「出発時間」「帰社時間」**（30分単位の選択肢）と、**「満足度（ランク）」**（25%〜100%の4段階）が自動的に出現します。
     * ここで入力された時間と満足度は、保存時に「商談内容」の欄へ `【外出時間】 09:00〜18:00 【満足度】 75%` の形式で自動的にマージされ、エクセル側の集計整合性を崩さない形で美しく記録されます。

2. **エクセルの仕様に準拠したプルダウン機能**:
   * **デザイン種別**:
     * エクセルのマクロや既存ルールに適合するよう、入力値は自由記述ではなくプルダウン選択式となっています。
     * 選択肢: `別注（新版）`, `別注（改版）`, `別注（再版）`, `SP（新版）`, `-`（なし）

3. **保存と自動採番・バックアップ**:
   * 入力後に「保存する」ボタンを押すと、バックエンドでエクセルの最終行を自動検出します。
   * 適切な**管理番号**を自動で発番した上で、安全にエクセルへ書き込みます。
   * 保存の瞬間に、元のエクセルファイルと同じ場所に `.bak` というバックアップファイルが自動生成されます。万が一のPCフリーズやネットワーク瞬断が発生しても、データ消失の恐れはありません。

---

### 👥 ④ 得意先一覧
登録されているクライアント（得意先）の基本情報やアプローチ実績を確認する画面です。

#### 🔹 主な機能と活用方法:
* **訪問実績の可視化**:
  * 各得意先の「過去の総訪問件数」や「最終訪問日」が一覧化されています。
* **ピンポイント履歴**:
  * 得意先を選択すると、その会社に対するこれまでのアプローチ履歴のみを抽出して確認できます。

---

### 📅 ⑤ カレンダー
カレンダー形式で、いつ・誰が・どの得意先へ訪問や連絡を行ったかを視覚的に管理・把握する画面です。

#### 🔹 主な機能と活用方法:
* **色分け表示**:
  * 日付ごとの枠内に関わった得意先名や活動内容（訪問・電話・社内など）が色分けされて配置されます。
* **行動計画・偏りのチェック**:
  * 得意先へのアプローチの偏りがないか確認する営業計画ツールとしても使用可能です。

---

### 📦 ⑥ デザイン検索
過去に蓄積されたデザイン依頼書、別注手配、SP手配などの案件情報を多角的に検索・管理する画面です。

#### 🔹 主な機能と活用方法:
* **クロスフィルター検索**:
  * 「デザインNo」「得意先名」「デザイン名」「デザイン種別」を組み合わせて複合検索できます。
* **動的フィルタリング**:
  * 特定の得意先を選ぶと、その得意先が過去に依頼した「デザイン種別」のみがプルダウンの選択肢として動的に絞り込まれます。
* **進捗ステータスバッジ**:
  * デザインのステータス（「進行中」「校了」「中止」など）が色分けされたバッジ（青・緑・赤など）で視覚的に表示されます。
* **タイムライン機能**:
  * デザイン依頼を選択すると、そのデザインの依頼から現在の進捗、関連する営業担当者のこれまでのアプローチ履歴が時系列カードで一覧表示されます。

---

### 🏢 ⑦ 量販店調査検索
店頭・量販店で行った競合状況調査や、自社製品の陳列状況などのフィールド調査結果を登録・検索する画面です。

#### 🔹 主な機能と活用方法:
* **フィールドデータの蓄積**:
  * 調査日、対象店舗、陳列商品、販促（POPなど）の状況を記録。
  * 店頭写真をアップロードまたはアップロード先を紐付けることができ、後から地域別や店舗別の陳列傾向を検索して資料として活用できます。

---

### ⚠️ ⑧ 競合他社情報
市場におけるライバル企業の動向（新商品の発売、価格改定、営業アプローチなど）を社内で記録・共有する画面です。

#### 🔹 主な機能と活用方法:
* **競合分析データの共有**:
  * 対象メーカー、商品カテゴリ、他社の推定価格、顧客から聞き取ったライバル製品の評価などをフォームに入力して共有します。
  * 検索することで、社内で「あの競合に対してどう立ち向かうか」を会議する際の確実な情報ソースとなります。

---

### 🚨 ⑨ クレーム対応
発生した不具合、顧客トラブル、納品ミスなどのクレーム案件を記録し、対応状況を追跡（トラッキング）する画面です。

#### 🔹 主な機能と活用方法:
* **確実なトラブル追跡**:
  * 発生日、得意先、クレーム詳細、緊急度（高・中・低）、対応状況（「未対応」「対応中」「解決済」）を入力します。
  * 完全に解決（解決済バッジ）するまで対応履歴が追跡されるため、トラブル対応の放置を防ぐ役割を果たします。

---

### 📈 ⑩ 分析・レポート機能群
入力された日報データを自動集計し、様々な角度から営業活動の効果や売上結果をグラフ・集計表で分析する画面群です。

* **📊 月次サマリー (`/reports/summary`)**:
  月ごとの活動量（訪問、電話、社内等）の集計表。営業月報の作成にそのままコピーできます。
* **👥 全メンバー分析 (`/analytics/members`)**:
  複数の営業メンバーが使用している場合、メンバー同士の活動件数比較、得意先への偏りの有無をチーム全体で分析します。
* **📈 分析・レポート (`/analytics`)**:
  重点顧客へのコンタクト比率や、時間外移動効率などの営業KPIを集計した詳細レポート。
* **💰 売上分析 (`/sales-analysis`)**:
  日報データと売上目標を紐付け、活動件数が売上達成率にどう影響しているか（活動と売上の相関）の予測・グラフ化を行います。

---

### ⚙️ ⑪ 設定
接続先エクセルのディレクトリ指定や、システムポート、バックアップの有効化、個人プロフィールの編集などを行う画面です。

---

### ☁️ ⑫ 【重要機能】オフライン動作と自動同期システム（SyncStatus）
サイドバーの最下部には、システムの接続状態（オンライン/オフライン）とデータ同期状況を示すインジケーター（SyncStatus）が表示されます。

```
 [サイドバー最下部]
 +-----------------------------+
 |  ☁️ オフライン (3件未送信)   |  <--- ネットワーク切断時の表示
 |  (クリックしてオンライン時に同期) |
 +-----------------------------+
```

#### 🔹 仕様とメリット:
* **社外や移動中での入力**: インターネットや社内ネットワークに接続されていない状態（オフライン）でも、日報の入力や下書き保存が可能です。データはブラウザ内に一時保存されます。
* **自動同期機能**: PCが社内ネットワーク（またはVPN）を検知してオンラインに復帰すると、インジケーターが「同期中...」に切り替わり、未送信データが自動的にバックエンドを介してExcel原本へ書き込まれます。
* **手動同期**: 同期を明示的に行いたい場合は、ステータスをクリックするだけでワンクリック同期が実行されます。

---

## 5. Excel連携およびデータ管理の仕様

本システムは、データベース（SQL等）を使用せず、**エクセルファイルを直接データベースの代わりとして使用**します。このため、以下の仕様と注意点をご理解の上ご利用ください。

### 📁 データの書き込み・読み込みプロセス
1. システムが起動すると、`config.json` に設定されたエクセルファイルの全行を読み込み、メモリ上にキャッシュします。
2. 新規追加や編集が行われると、EXE化されたバックエンド側で高速データ処理（`pandas`等）およびエクセル操作（`openpyxl`等）を実行し、書式やエクセル関数・マクロを保持したまま書き込みを実行します。

### 🛡️ 自動バックアップ仕様
書き込みエラーやネットワーク切断によるファイル破損を防ぐため、保存処理の直前に以下の処理を行います。
1. 対象エクセルファイル（例：`日報_2026.xlsm`）のコピーを作成。
2. コピーしたファイルを `日報_2026.xlsm.bak` という名前で同じフォルダに保存。
3. 万が一、書き込み処理中に予期せぬエラーが発生した場合、この `.bak` ファイルの拡張子を `.xlsm` に戻すことで、即座に保存直前の状態へ復旧できます。

> [!CAUTION]
> **エクセルファイルの同時編集に関する重要なお願い**
> 本システムでデータを登録する際、**対象となるエクセルファイルを他のPC（または自分自身）が直接Excelソフトで開いた状態（編集ロック状態）にしないでください。**
> エクセルがロックされていると、システムが保存処理を行う際に書き込みエラー（Permission Denied / Permission Error）が発生します。
> データ書き込み時は、必ず対象のエクセルファイルを閉じておくよう周知徹底をお願いいたします。

---

## 6. トラブルシューティング

システムが正常に動作しない場合は、以下の手順に沿って対応してください。

### ❓ 1. 起動用EXEをダブルクリックしてもブラウザが開かない、または画面が真っ白
* **原因 A**: セキュリティソフトによるブロック。
  * **対策**: 社内配布用のEXEファイルが、個人のPCのセキュリティ対策ソフト（Windows Defenderなど）によって一時的にブロックされている場合があります。セキュリティの警告が表示された場合は「詳細情報」から「実行」を許可してください。
* **原因 B**: バックグラウンドで別プロセスが同じポート（フロント:3000、バック:8001）を占有している。
  * **対策**: 開いている黒いコマンドプロンプト画面を一度すべて閉じ、PCを再起動してから再度起動してください。

### ❓ 2. 画面に「Failed to fetch」または「500 Internal Server Error」と表示される
* **原因 A**: バックエンド（EXE）サーバーが停止している。
  * **対策**: 黒いコマンドプロンプト画面が閉じられていないか確認してください。閉じている場合は、再度EXEファイルを実行します。
* **原因 B**: エクセルファイルが別の人間によって直接開かれてロックされている。
  * **対策**: 対象のエクセルファイルが閉じられているか確認してください。
* **原因 C**: 設定されている共有ネットワーク上のパス（`\\Asahipack02\` など）にPCがアクセスできていない。
  * **対策**: 社内ネットワーク（VPN含む）に正常に接続されているか、エクスプローラーから共有フォルダに直接アクセスできるかを確認してください。

---

## 7. 管理者・保守担当者向け情報

システムの動作設定は、配布フォルダ内の `config.json` ファイルで制御されています。

### ⚙️ `config.json` の設定例

```json
{
  "excel_file_path": "D:\\google_cli\\daily-report-system-2026\\daily_report_template.xlsm",
  "backup_enabled": true,
  "port": 8001
}
```

* `excel_file_path`: 読み書きするエクセルファイルの絶対パスを指定します。社内共有フォルダを対象にする場合は、`\\\\Asahipack02\\Shared\\日報_2026.xlsm` のように、Windowsのパス形式に合わせてバックスラッシュ（`\`）を二重にして記述してください。
* `backup_enabled`: 自動バックアップの有効（`true`）/ 無効（`false`）を切り替えます。データ保護のため、原則 `true` のままで運用してください。

### 📅 新年度（年度変更時）の切り替え手順

年度が新しくなり、日報の保存先エクセルファイルを切り替える場合は、以下の手順に従って設定を行ってください。

1. **新しい年度用エクセルファイルの準備**:
   * 前年度のファイル（またはひな形テンプレートファイル）をコピーし、新しい年度の名前（例：`日報_2027.xlsm`）で共有サーバーまたはローカルに保存します。
2. **`config.json` の書き換え**:
   * システムフォルダ内の `config.json` をテキストエディタ（メモ帳など）で開きます。
   * `"excel_file_path"` の値を、先ほど準備した新しいファイルのパスに書き換えます。
     * 例（2026年から2027年への変更）：
       ```json
       "excel_file_path": "\\\\Asahipack02\\Shared\\2027年度\\日報_2027.xlsm"
       ```
     * ※バックスラッシュ（`\`）は必ず二重（`\\`）で記述してください。
3. **システムの再起動**:
   * すでに起動している場合は、ブラウザと `DailyReportServer.exe`（黒い画面）を一度完全に閉じます。
   * 改めて `DailyReportServer.exe`（または起動用ショートカット）を起動します。
   * ダッシュボード画面の上部に表示される「アクティブファイル」が新しい年度のファイルパスに切り替わっていれば、設定完了です。
