# CHANGELOG

## [2026-07-14]
### 追加 (Added)
- **企画課ビューワーとの連携機能の更なる拡張**:
  - **ステータス変更通知 (タブ復帰時の差分検出)**:
    - ユーザーがブラウザタブをアクティブに戻した際（`visibilitychange`）に、自動的に前回のスナップショットとの差分をチェックする `useViewerNotifications` フックを新規実装しました。
    - 定期的なポーリングを排除し、タブ復帰時（最低5分以上の間隔）のみ通信を行うようにスロットリングすることで、ビューワーサーバーへの不要なリクエスト負荷をほぼゼロに抑えています。
    - ステータスの遷移（進行中・完了・却下・入稿）、新しいカンプのアップロード、新規依頼の追加を検知した際、`react-hot-toast` によるトースト通知を自動で行います。
  - **遅延ロード型 PDF仕様書プレビューモーダル**:
    - デザイン依頼に紐づくPDF仕様書を日報システム内でプレビューできる `PdfPreviewModal.tsx` を新規実装しました。
    - モーダルが開かれるまで iframe は生成されず、閉じられた際にはDOMから完全に破棄（アンマウント）することでブラウザのメモリおよび通信負荷を最小限に抑えています。
    - 「デザイン依頼検索」画面の各行にPDFプレビューアイコンを新設し、別タブに遷移することなくその場で仕様書を確認できるようになりました。
  - **デザイン進捗ページのビューワーデータ統合 (デュアルビュー)**:
    - 「デザイン進捗管理」画面において、Excelの日報履歴（左カラム）と、企画課ビューワーのリアルタイム進行データ（右カラム）を並列表示する2カラム構成のデュアルビュー表示を実装しました。
    - 企画課ビューワー側の最新進捗ステータス、担当デザイナー名、納品予定日、仕様書PDFプレビュー、および最新カンプ画像のサムネイルがその場でリアルタイム表示されます。
    - React Query を活用して企画課のデータ取得（`useViewerDesignRequests`）を 10分間 キャッシュし、頻繁なページ遷移時の負荷を極限まで低減しました。
  - **未入力の重要デザイン依頼に対する日報起票警告 (ホーム画面)**:
    - 企画課ビューワーに登録されている案件のうち、種別が「ＳＰ（ロール印刷）」または「フルオーダー」である重要デザインに関して、日報に一度も登録（起票）されていない自分の担当案件を自動検出する警告コンポーネント（`UnfilledImportantDesigns.tsx`）を実装しました。
    - 条件に合致する未入力案件がある場合のみ、ホーム画面（ダッシュボード）最上部に警告カードが出現し、右側の「日報作成」ボタンをクリックするだけで得意先やデザイン情報を自動入力した状態で日報作成モーダルを起動できます。

## [2026-07-13]
### 追加 (Added)
- **企画課デザイン作成依頼管理システム（ビューア）とのデータ連携機能の実装と改善**:
  - バックエンド（FastAPI）に、ブラウザのセッションCookieを中継転送するプロキシAPI（`routes_proxy.py` / `/api/proxy/design-requests`）を新規実装し、CORSの制約を回避しながら企画課ビューア（`192.168.1.5:8888/api/documents`）の最新依頼データを安全に取得可能にしました。
  - フロントエンドの「新規日報入力モーダル（`NewReportModal.tsx`）」および「日報一括入力画面（`batch/page.tsx`）」に、「デザイン作成依頼（企画課）から選択」プルダウンを追加しました。現在開いているExcelファイル名から担当営業名（名字）を自動判別し、表記揺れを許容したうえで「自分自身の進行中の依頼案件」のみを自動フィルタリングしてリストアップするスマートな絞り込みを実装しました。
  - 新規連携のプルダウン上に、キーワードによるリアルタイム検索（フィルタリング）用インプットフィールドを新設しました。ビューアのデータが将来的に膨大になった場合でも、依頼番号、得意先名、依頼内容の一部を入力するだけで、瞬時に対象案件を絞り込み選択できるよう利便性を強化しました（個別新規・一括入力の双方に対応）。
  - プルダウンの表示において、ステータスが **「出稿（`inSubmission`）」** になっている案件はすでに日報作成の準備段階を終えたとみなし、選択肢（候補）に表示させないフィルタ処理を追加しました。
  - 依頼書選択時、「依頼番号のハイフン以前の最初の6桁（枝番なし）」を `デザイン依頼No.` に反映し、「依頼内容」を `デザイン名` に反映する2項目のみの書き込み（上書き）自動補完処理を実装しました。得意先や種別、進捗状況などの他のフォーム項目は日報側の既存の選択が維持されます。
  - 企画課ビューアのセッション切れ等（401 Unauthorized）の際は、選択エリアに「企画課ビューアにログインしていません。[ログインする]」という案内アラートとリンクを表示する親切なUI設計としました。
  - 「デザインデータギャラリー（`DesignImageGallery.tsx`）」において、共有サーバー（`Asahipack02`）から読み込んだローカル画像ファイルリストの先頭に、企画課ビューアから取得したカンプ画像（存在する場合）をマージ結合し、直接プレビュー表示できる機能を追加しました。ここでは「出稿（`inSubmission`）」案件の画像も除外せず表示対象のまま維持し、常に最新のプレビュー画像にアクセスできるよう担保しました。
- **セキュリティ・堅牢性の向上 (Security & Stability Updates)**:
  - ローカルIP等の HTTP 接続時（非セキュアコンテキスト環境）において、ブラウザの `crypto.randomUUID()` が無効化されシステムがクラッシュする問題に対応するため、RFC4122準拠のフォールバック型ID生成関数 `generateUUID()` を [reportUtils.ts](file:///c:/Users/asahi/.gemini/antigravity/playground/pyro-eclipse-2026/daily-report-system-2026/frontend/src/lib/reportUtils.ts) に実装し、一括入力およびオフライン保存処理で利用するように書き換えました。
  - 営業担当者の名字抽出および部分一致検索のロジックにおいて、ファイル名が未選択（null）の場合やビューア案件に担当者が割り当てられていない場合に発生する TypeScript の Null/Undefined 参照エラーを防止し、画面フリーズを引き起こさない堅牢なチェック処理に改修しました。

## [2026-07-10]
### 追加 (Added)
- **月次活動サマリーの「日別活動一覧」における活動詳細展開（アコーディオン）機能の実装**:
  - 日別活動一覧の各日にアコーディオン開閉トグルを追加し、展開時にその日の活動詳細（顧客名、行動内容、商談内容、デザイン提案情報、直送先情報）をカード形式で美しく表示するUIを構築しました。
  - バックエンドAPI `/api/stats/monthly-summary` を拡張し、日別活動データの中に該当する日報の全活動詳細をリスト化して返すようにしました。
- **月次活動サマリーの活動詳細からのデザイン画像プレビュー機能の追加・改善**:
  - アコーディオンで展開される活動詳細内のデザイン提案情報に、デザイン依頼No.がある場合は画像プレビュー用のアイコンボタンを表示し、クリックすると関連するデザイン画像の一覧をモーダル表示できる機能を追加しました。
  - 画像ボタンを視認性の高いピンク調のスタイル（ピンク背景・枠線）に変更し、アイコンの隣に「画像」という文字ラベルを表示して、一目で画像が閲覧可能であることがわかるようUIを改善しました。
  - バックエンドでのExcelからの読み込み時にデザインNo.に付与される浮動小数点数（`.0`）を、バックエンドAPI（`routes_stats.py`）およびフロントエンド側双方で自動除去して検索APIを呼び出すようにし、画像のヒット精度を担保する修正を行いました。
- **統一デザイン画像プレビューモーダルの新規作成とシステム全体のプレビューUI統合**:
  - 左側に大きな画像プレビューエリア、右側に「バリエーション・履歴」リストを配置した、統合された2カラムレイアウトのプレビューモーダル（`DesignImagePreviewModal.tsx`）を新規作成しました。
  - モーダルが開いた際、自動的に画像リストを更新日時（`mtime`）の降順（新しい順）にソートし、デフォルトで「更新日時が最新の画像」を最初（インデックス0）としてプレビュー表示するロジックを実装しました。
  - 履歴リストも更新日時順に並び、現在選択されている画像が青枠でハイライトされます。
  - リスト内のPDFファイルを選択した場合は、プレビュー欄にPDFの別ウィンドウオープン用の案内ボタンを表示する親切なUIにしました。また、小画面表示時には自動的に縦並びにフォールバックするレスポンシブデザインを適用しました。
  - この新規コンポーネントを、①日報詳細モーダル（`ReportDetailModal.tsx`）、②月次サマリー画面（`summary/page.tsx`）、③デザイン検索画面（`design-search/page.tsx`）、④得意先詳細画面（`customers/detail/page.tsx`）の4画面すべてにインポート・統合し、UIとロジックを完全に統一しました。
- **「一日社内」強調および「訪問地域（エリア）」のバッジ表示**:
  - 訪問件数が0回の日は「一日社内」と自動判定し、日付の横に「一日社内」バッジを表示するとともに、行全体の背景色を薄い暖色（ピーチ/薄オレンジ）にハイライトして視認性を向上させました。
  - 訪問件数が1件以上の日は、その日の訪問アクションに紐づくエリア（地域）をユニーク化（重複排除）し、日付の横に青色のエリア名バッジとして並べて表示する機能を追加しました。これに合わせ、バックエンドAPIもアコーディオン用活動詳細リストの中に `area` 情報を付与して返すように修正しました。
  - 印刷時（印刷/PDF保存）には、アコーディオンが展開されていても詳細部分は非表示（`print:hidden`）の状態で、元のすっきりとしたサマリー表のままレイアウトが崩れずに印刷・保存できるようCSSを調整しました。

## [2026-07-07]
### 修正 (Fixed)
- **日報一覧でのページネーション切り替え時のモーダル表示バグの修正**:
  - `page.tsx` において、2ページ目以降の日報クリック時に、クリックした行に対応する正しい詳細モーダルが開かず、1ページ目の同じインデックスのデータが表示されてしまう不具合を修正しました。
  - `handleRowClick` に渡すインデックスにページネーションのオフセット（`(currentPage - 1) * itemsPerPage`）を加算するように修正しました。

## [2026-07-01]
### 追加 (Added)
- **新規作成・一括登録への「下書き破棄」ボタンの追加**:
  - `NewReportModal.tsx`（新規作成）および `batch/page.tsx`（一括登録）に、「下書きを破棄」ボタンを新設しました。
  - 下書きデータが復元されているときにヘッダー部分にバッジ/ボタンを表示し、クリックすると `clearDraft()` でストレージからデータを削除し、フォームの状態を初期状態（デフォルト）へリセットするロジックを実装しました。
- **自由入力された顧客名からの既存デザイン自動検索・選択機能の追加と改善**:
  - `NewReportModal.tsx`（新規作成）、`batch/page.tsx`（一括登録）、`EditReportModal.tsx`（編集）において、顧客名をサジェストから選択せず手動で自由入力（例：「さちふる」）した場合でも、既存のデザイン情報をロードしてプルダウンから選択できるようにしました。
  - フロントエンドで顧客マスタ（`customers`）に存在しない名前が入力された場合も対応するため、バックエンドの `get_designs` APIに「得意先CDでの不一致時に、得意先名・直送先名・訪問先名から部分一致でデザインを逆引きする」フォールバック検索ロジックを実装しました。これにより、マスタ未登録の新規顧客や店舗であっても過去のデザイン案件を確実に引き当てて選択・紐付けできるようになりました。
- **自由入力された顧客名からの「エリア」自動補完機能の追加**:
  - 新規作成、一括登録、編集モーダルにおいて、顧客名を手動で直接入力（自由記載）した場合でも、過去の履歴から最新の「エリア」を逆引きして自動入力欄へ補完する機能を追加しました。
  - バックエンドに過去の営業日報からエリアの記録を逆引きして返却する API `/api/customers/suggest-area` を新設し、フロントエンドでの自由入力確定時の `onBlur` イベントと連携させました。

### 修正 (Fixed)
- **下書き自動一時保存処理へのデバウンス（遅延書き込み）の導入によるパフォーマンス最適化**:
  - `useLocalStorageDraft.ts` 内での `saveDraft` に 1秒間のデバウンス（遅延）処理を追加しました。文字入力のたびに発生していた `localStorage.setItem` の過剰な実行を防ぎ、入力が停止したタイミングでまとめて書き込むように最適化しました。
  - モーダルが閉じられる（アンマウント）際、または一時保存対象のキーが変更される際に、保留中の一時保存データが即座に `localStorage` へフラッシュ（書き込み）されるクリーンアップ処理を導入し、データ消失を防ぐ仕組みを追加しました。
  - 日報詳細（`ReportDetailModal.tsx`）および編集（`EditReportModal.tsx`）の上長コメント・返信入力の自動保存（`saveCommentDraft`）についても、同様に `useRef` と `useEffect` を用いた1秒間のデバウンス保存とアンマウント時の即時書き込み処理を実装し、書き込み負荷を大幅に削減しました。
- **フォーム消去時の下書き自動削除機能の強化**:
  - `EditReportModal.tsx` において、ユーザーが入力した全基本項目を手動で消去した際に、元のExcelデータと異なる（変更あり）と判定されて「すべて空の下書き」が自動保存されてしまう不具合を修正しました。変更フラグだけでなく、主要項目（訪問先名、行動内容、商談内容、面談者、提案物、次回プラン、競合他社情報、エリア、デザイン依頼No.）にデータが存在するかを確認する `hasData` チェックを導入し、フォームが空になった場合は `clearDraft()` で下書きを自動削除するよう改善しました。
  - `NewReportModal.tsx` の自動保存トリガー `hasData` チェックについて、`次回プラン`・`競合他社情報`・`エリア`・`デザイン依頼No.` が判定対象から漏れていたため、これらの項目のみを入力・変更した際にも正しく一時保存が走り、またすべての項目が空になった際には下書きが完全にクリーンアップされるよう修正しました。
  - `batch/page.tsx` において、日付の変更 (`date !== today`)、および `エリア`・`デザイン情報`（種別、名、進捗、依頼No.）などの変更があった場合でも正しく下書きが自動保存され、また全てが初期状態に戻った時には下書きが自動削除されるよう `hasData` チェックの条件を修正しました。
- **コメント一時保存（下書き）のクリア処理の改善**:
  - `ReportDetailModal.tsx`（詳細）および `EditReportModal.tsx`（編集）のコメント自動一時保存機能において、ユーザーが手動でコメントや返信欄の内容を全消去して空にした際に、元のコメントが空でなければ「空文字列」が下書きデータとして保存されてしまい、次回オープン時に元の内容が表示されなくなる不具合を修正しました。入力内容が元の値と同じに戻った場合だけでなく、入力内容が空（`""`）になった際にも `clearCommentDraft` を呼び出し、下書きを即座に破棄するようにロジックを修正しました。
  - イベントハンドラ（`handleChange` やラムダ式）の型整合性を向上させ、すべての関数において明示的な戻り値型（`: void`）を定義しました。

## [2026-06-30]
### 追加 (Added)
- **日報詳細モーダルでのデザイン画像プレビュー機能の実装**:
  - 日報詳細モーダル（`ReportDetailModal.tsx`）内の「デザイン案件」セクションにおいて、デザイン依頼No.の右側に画像検索用のアイコンボタンを新設し、デザイン検索と同様の画像一覧モーダルと拡大ライトボックスで関連画像をその場で確認・プレビューできる仕組みを追加しました。

### 修正 (Fixed)
- **上長コメント通知エリア（UnreadComments.tsx）の不具合修正とEXE再パッケージング**:
  - `frontend/src` にあった、上長コメントの折り畳みボタン、返信時のキャンセルボタンのバグ修正およびコメント押下時の不要なモーダル遷移防止対応を、フロントエンドの再ビルド (`npm run build`) を実行して反映しました。
  - 静的ビルドアセット (`frontend/out`) をバックエンド用の `static/` フォルダへコピーし、PyInstaller (`py -m PyInstaller DailyReportServer.spec`) を実行して配布用EXE `DailyReportServer.exe` を再コンパイルしました。
  - デスクトップの展開フォルダ `C:\Users\asahi\Desktop\DailyReportSystem_20260627` 配下の `DailyReportServer.exe` および ZIP パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260624.zip` を更新し、ポートのバッティング回避のため以前のバックグラウンドプロセスを停止しました。

## [2026-06-29]
### 修正 (Fixed)
- **日報編集モーダルでの状態更新バグ（Stale Closure）の修正**:
  - `EditReportModal.tsx` の `handleSubmit` において、`saveStatus` ステートの古いクロージャ値を参照して `finally` ブロックで `setSubmitting(false)` が誤作動する不具合を、ローカル変数 `succeeded` で成功状態を管理することで修正しました。
  - API の応答速度が高速な環境において、送信・保存中のアニメーション演出（`sending` -> `writing` -> `backup`）がスキップされてしまう問題を、`Promise.all` を用いて最低表示時間（送信中400ms、書き込み中500ms、バックアップ中400ms）を担保する並行 Promise 処理を導入することで解決しました。
- **日付入力の固定化によるバリデーション強化**:
  - `NewReportModal.tsx` および `EditReportModal.tsx` の日付入力フォームを `<input type="text">` から標準の `<input type="date">` へ変更し、任意の無効な日付やテキストの入力を完全に防ぐ設計にしました。
  - Excelへの保存形式（`YY/MM/DD`）とDatePickerで使用する標準形式（`YYYY-MM-DD`）の双方向変換処理を実装しました。
- **日報編集モーダルでの基本情報下書き（一時保存）機能の実装**:
  - `EditReportModal.tsx` に `useLocalStorageDraft` を導入し、商談内容などの主要項目の入力内容を管理番号に紐づくキーで自動一時保存・復元できるようにしました。
  - 下書きデータが復元された際はヘッダー部分に復元中バッジと破棄ボタンを表示し、保存（更新）成功時には自動で下書きをクリーンアップするロジックを実装しました。
- **日報一括入力画面（/reports/batch）での保存アニメーション追加**:
  - 一括保存ボタンに、個別モーダルと同様のプレミアムアニメーション（プログレスバー、シマー効果、および段階的なテキスト遷移）を追加しました。
  - `handleSubmit` に `minimumDisplayPromise` を導入し、通信速度に関わらず「送信中 → 書き込み中 → バックアップ中 → 保存完了」の状態遷移がしっかりと視認できるよう修正しました。

### 変更 (Changed)
- **配布用パッケージ（ZIP）およびEXEの更新**:
  - フロントエンドの最新ビルド成果物を `static/` ディレクトリにコピーし、PyInstaller により `DailyReportServer.exe` を再コンパイルしました。
  - デスクトップ上の配布用パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260624.zip` 内、およびデスクトップ展開済みフォルダ `C:\Users\asahi\Desktop\DailyReportSystem_20260627` 配下の `DailyReportServer.exe` を更新しました。

## [2026-06-25]
### 追加 (Added)
- **プレミアムな「保存中」アニメーション（ビジネス仕様）の実装**:
  - 日報保存・更新時の非同期処理中に、信頼感を与えるダークスレート色を基調とした進行状況テキスト、シマー（光沢）効果、極細プログレスラインをボタン内部に表示するプレミアムUI/UXを `NewReportModal.tsx` および `EditReportModal.tsx` に実装しました。
  - 保存完了時にはボタンが上品なティールグリーンへと変化し、チェックマーク（`✓`）がバウンドして出現する演出を追加しました。
  - アニメーション用のCSSクラスを `globals.css` に追加しました。
  - 最新のソースコードを含めたNext.js静的アセットの再エクスポート、およびPyInstallerによる `DailyReportServer.exe` のパッケージング（再ビルド）を完了しました。

### 変更 (Changed)
- **型定義の集約とリファクタリング**:
  - `api.ts` などに散在していた型定義（`Report`, `Customer`, `ExcelFile` など）を `types/report.ts` に新規集約・定義しました。
  - プロジェクトルールに従い、`interface` 定義を `type` 定義へと書き換えました。
- **ESLintエラーおよびビルドエラーの解消**:
  - フロントエンド全体の `any` 使用エラー（`@typescript-eslint/no-explicit-any`）を一括で修正し、適切な型定義や `unknown` に変更しました。
  - 状態変更のバグ（`set-state-in-effect`）や宣言前アクセスなどのReact Hook・JavaScriptエラーを `useMemo` による計算や非同期ロードへの修正で解消し、ESLintを完全にパスするようにしました。

## [2026-06-24]
### 追加 (Added)
- **上長コメント・返信欄の入力途中データの自動一時保存・復元機能**:
  - コメントおよび返信の入力中に別の画面に遷移した場合に入力内容が失われないよう、`sessionStorage` を利用した自動一時保存（下書き）機能を実装。
  - 日報詳細モーダルおよび日報編集モーダルを開いた際、該当する管理番号の下書きがある場合は自動で入力欄に復元するロジックを実装。
  - 一時保存データが復元された際、入力欄ラベルの横に「一時保存データを復元中」と表示する視覚的インジケーター（アニメーション付きバッジ）を追加。
  - 復元された下書きをクリアして元の値（サーバー上の値）に戻すための「下書きを破棄」ボタンをインジケーターの横に配置。
  - Excelへの保存成功時（詳細モーダルでの `onBlur` 自動保存、または編集モーダルでの「更新」ボタン押下による保存完了時）に、該当の下書きデータを自動でストレージからクリーンアップする仕組みを追加。

## [2026-06-10]
### 追加 (Added)
- **上長コメント通知エリアの開閉トグル機能追加**:
  - ホーム画面の未読上長コメント通知エリアにおいて、未読コメントが3件より多い場合に「すべて表示」および「折りたたむ」を行える開閉トグルボタンをヘッダーの右側に追加しました。
  - 初期表示は3件のまま維持され、4件以上の未読コメントがある場合のみトグルボタンが表示されるように制御しました。
- **デザイン画像取得処理の高速化によるタイムアウト解消**:
  - ホーム画面の「デザインデータ一覧」で画像を取得する `/api/images/list` エンドポイントにおいて、共有ネットワークドライブ上のフォルダ走査を `os.walk` + `os.path.getmtime` から `os.scandir` + `entry.stat().st_mtime` を使ったスタックベースの高速走査に変更しました。
  - これにより、ネットワーク通信の往復回数（レイテンシによる待ち時間）が劇的に削減され、ファイル数・フォルダ数が大量にある場合でもタイムアウトを起こさずに高速に読み込めるよう改善しました（最大3階層の探索深度は維持）。

## [2026-06-09]
### 追加 (Added)
- **日報一括入力の日付選択改善**:
  - 日付入力枠の右側にカレンダーアイコンを追加し、カレンダー（DatePicker）から日付を選択できるようにしました。
  - テキスト入力されている `YY/MM/DD` 形式の日付とカレンダー用の `YYYY-MM-DD` 形式を双方向に同期・変換するヘルパー関数 `convertYYMMDDToYYYYMMDD`, `convertYYYYMMDDToYYMMDD` を `reportUtils.ts` に追加しました。
  - 追加した日付変換処理に対する網羅的なユニットテストを `reportUtils.test.ts` に実装しました。

### 変更 (Changed)
- **配布用パッケージ（ZIP）の更新**:
  - 最新のフロントエンド修正を含む production ビルドを `static/` ディレクトリに同期し、PyInstaller で最新の `DailyReportServer.exe` を再ビルドしました。
  - デスクトップ上の配布用パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260526.zip` 内の実行ファイル（`DailyReportServer.exe`）を更新し、ZIPファイルを再パッケージ化しました。

## [2026-06-05]
### 追加 (Added)
- **ホーム画面デザインデータギャラリーの改善**:
  - 同じデザイン依頼番号（画像名に含まれる5桁以上の連続する数字）の重複を排除し、最新の画像（更新日時 `mtime` が最も新しいもの）のみを代表として一覧に表示するようにしました。
  - 画像カードのデザインを改善し、切り抜きを排除して全体が表示されるようにし（`aspect-[3/4]` & `object-contain`）、画像ファイル名を常時表示するレイアウトに更新しました。
  - 画像をクリックした際に起動する「スプリットビュー・プレビューモーダル」を追加しました。左側に最新の拡大メイン画像、右側に関連するバリエーション画像や過去バージョンのサムネイル一覧を縦に並べ、サムネイルをクリックすることでメイン画像を切り替えて表示できるインタラクティブなUIを実装しました。
  - 画像プレビューモーダルが親要素の CSS `transform` アニメーションの影響で画面の「スクロール中盤」にずれて配置されてしまうバグを、React Portal（`createPortal`）を導入して `document.body` 直下にレンダリングすることで修正し、常に今見ている画面の中央に即時表示されるようにしました。また、モーダル表示中は背景の不要なスクロールをロックする制御を追加しました。
  - バックエンドの画像取得API（`get_design_images`）において、ファイル数が100件を超えた時点でスキャンを打ち切るブレーク条件があったため、直下フォルダに画像が多数ある場合にサブフォルダ（フォルダ内のフォルダ）の探索に入らずサブフォルダ内のデザイン画像が読み込まれなくなっていたバグを修正しました。スキャン制限を撤廃してサブフォルダも含めて全走査し、最新の最大500件を返すように最適化しました。
  - **【追加】巨大ディレクトリ走査時のタイムアウトバグの修正**: スキャン対象のフォルダ配下に大量のファイルがある場合（例：見上さんのフォルダには9,309件存在）、ネットワークドライブ経由での全再帰スキャンに数十秒〜数分かかり、接続タイムアウト（30秒）を引き起こしていた問題を修正しました。画像取得処理（`get_design_images`）の `os.walk` において最大探索深度（`max_depth=3`）を適用し、実務で使われない超深層のフォルダ走査を打ち切ることで、処理時間をミリ秒単位に高速化し、タイムアウトを完全に解消しました。
  - デザイン画像情報（`DesignImage` 型）に更新日時 `mtime?: number` プロパティを追加定義しました。
- **個別の得意先の月別活動数可視化機能**:
  - 得意先詳細画面（`/customers/detail`）に「月別活動数」タブを追加しました。
  - `recharts` の `BarChart` を用いて、月別の活動推移（訪問、電話、デザイン提案、クレーム、提案物）を視覚的にわかりやすい棒グラフで表示するようにしました。
  - 各月の活動数の詳細数値を一覧で確認できる「月別活動データ詳細」テーブルを追加しました。
  - フロントエンドで月別データを集計するロジック（`aggregateMonthlyActivities`）を `frontend/src/lib/activityUtils.ts` に新規作成しました。
  - `MonthlyActivityStats` 型定義を `frontend/src/types/activity.ts` に新規作成し、型定義集約のルールに従いました。
- **活動集計ユーティリティのユニットテスト**:
  - `frontend/src/lib/activityUtils.test.ts` を新規作成し、Node.js 標準の `node:test` および `tsx` を用いて集計ロジックの動作を検証する網羅的テストを実装しました。
- **得意先詳細画面への「得意先一覧に戻る」ボタンの追加**:
  - 得意先詳細画面のヘッダーに、一覧画面へ遷移できる「得意先一覧に戻る」リンクボタンを追加しました。

### 変更 (Changed)
- **配布用パッケージ（ZIP）の更新**:
  - フロントエンドの最新ビルドを `static/` ディレクトリに同期し、PyInstaller で最新の `DailyReportServer.exe` を再ビルドしました。
  - デスクトップ上の配布用パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260526.zip` 内の実行ファイル（`DailyReportServer.exe`）を上書き更新しました。

## [2026-06-04]
### 修正 (Fixed)
- **重点顧客マスタ取得時の列指定バグの修正**:
  - `backend/main.py` および `release_pkg/backend/main.py` の `get_priority_customers` において、`col_customer_name` がインデックス1（直送先CD.）を参照していたバグを修正。
  - カラム名に「得意先名」が含まれるものを動的に走査・特定し、見つからない場合もフォールバック先としてインデックス2（得意先名）を参照するように修正。
  - これにより、得意先CDに対する名称（`幸南食糧（株）` など）が正しく紐づき、フロントエンドでの重点顧客のマッチングおよび全日報レコード（NaNフラグを含む全活動数）の集計・マトリクスへの反映が正確に動作するよう改善しました。

### 変更 (Changed)
- **配布用パッケージ（ZIP）の更新**:
  - フロントエンドの最新ビルドを `static/` ディレクトリに同期し、PyInstaller で最新の `DailyReportServer.exe` を再ビルド。
  - デスクトップ上の配布用パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260526.zip` 内の実行ファイルを上書き更新。

## [2026-06-03]
### 追加 (Added)
- **コメント・承認チェック更新時の楽観的ロック（排他制御）の実装**:
  - 同時編集によるデータ上書き（ロストアップデート）を防止するため、上長コメント・コメント返信（`/api/reports/{management_number}/comment`）および承認ステータス（`/api/reports/{management_number}/approval`）の各更新APIに楽観的ロックを導入。
  - バックエンド（`backend/main.py` および `release_pkg/backend/main.py`）にて、処理開始時にExcelから最新の値を読み込み、フロントエンドから送信された編集開始時の値（`original_values`）と比較して差分がある場合に `409 Conflict` エラーを返却するロジックを実装。
  - 承認チェックの比較時には、表記ブレ（Wingdingsの `ü`, `✓`, `済` など）を同一の承認済状態として正規化する処理を追加。
  - フロントエンド（`frontend/src/lib/api.ts` および `ReportDetailModal.tsx`）にて、リクエスト時に元の `report` オブジェクトを `original_values` パラメータとして送信するよう拡張。

### 修正 (Fixed)
- **release_pkg バックエンドでの例外処理バグの修正**:
  - `release_pkg/backend/main.py` の `update_report_comment` および `update_report_approval` において、`except HTTPException: raise` が不足しており、楽観的ロック衝突時の 409 エラーが汎用例外キャッチにより 500 エラーに変換されてしまっていた不具合を修正。

### 変更 (Changed)
- **配布用パッケージ（ZIP）の更新**:
  - フロントエンドの最新ビルド（Next.js SSG）を `static/` ディレクトリにコピーし、PyInstaller を用いて最新の `DailyReportServer.exe` を再ビルド・更新。
  - デスクトップ上に最新の配布用パッケージ `C:\Users\asahi\Desktop\DailyReportSystem_20260526.zip` を再作成してエクスポート。

## [2026-05-26]
### 追加 (Added)
- **得意先詳細画面からのデザイン画像プレビュー機能追加**:
  - 得意先詳細画面（`/customers/detail`）の「デザイン案件」タブ内にあるデザイン案件一覧において、各デザインNo.の横にピンク色の「画像プレビュー（ImageIcon）」ボタンを設置しました。
  - ボタンをクリックすることで、デザイン検索画面と同様に関連画像を非同期に検索・取得し、モーダルおよびライトボックス形式でその場で画像を美しくプレビュー表示できる機能（前後の画像スライド切り替え、ダウンロード、PDF表示等）を完全移植しました。

### 修正 (Fixed)
- **日報編集モーダルでの保存失敗エラー（管理番号型ミスマッチによるバグ）の修正**:
  - `EditReportModal.tsx` から日報を更新（`POST /api/reports/{management_number}`）する際、Excel シート内の「管理番号（A列）」が文字列型（`str`）や浮動小数点型（`float`）で保存されている場合に、厳密比較 `cell_value == management_number` で行の検索に失敗し、404 エラー（`日報の更新に失敗しました`）になってしまうバグを修正しました。
  - バックエンド（`backend/main.py`）の `update_report` や `update_report_comment` などの各エンドポイントにて、セルの値を安全に数値キャストして比較するロジック（`int(float(str(val)))`）へ改善し、Excel 側の型定義の差異に関わらず確実に一致させて正常に保存できるよう対策しました。
- **日報編集エラーハンドリングの強化とトーストメッセージの親切化**:
  - `EditReportModal.tsx` の送信エラー catch ブロックにて、単に一律で `日報の更新に失敗しました` と表示するのではなく、サーバーから返却された具体的なバリデーションエラーや例外詳細（`error.response?.data?.detail`）をトーストメッセージに直接表示するように改善しました。これにより、万が一再度不具合が発生した際にも、現場のユーザーが直ちにエラー原因を特定・報告できるようになりました。
- **ホーム画面の通知エリアで「返信」ボタン押下時に「商談内容」が表示されなくなっていた不具合を修正**:
  - ホーム画面の未読上長コメント通知エリアにて、「返信」ボタンを押してインライン入力フォームを展開した際、本来表示されるべき「その日報本人の商談内容（Excelの商談内容カラム）」が表示されなくなっていた不具合（過去のコード競合により表示JSXが消失していた問題）を修正。返信対象の日報内容を確認しながら回答を入力できるよう、表示領域を完全に復旧させました。
- **ホーム画面の上長コメントに関する「既読」処理と「保存中/送信中」ローディング状態の表示修正**:
  - ホーム画面の「既読」ボタンをクリックした際、Excelの「コメント返信欄」（X列）に `"確認済み"` が直接書き込まれていた不具合を修正。代わりに「既読チェック」カラム（AC列）に対して `"ü"`（Wingdingsの「✓」）を正しく書き込むよう `updateReportApproval` API を用いた処理に変更し、返信欄を汚さずに既読処理を行えるよう改善しました。
  - ホーム画面の通知エリアでの「返信」の送信や「既読」ボタン押下時に、ローディング表示（「保存中...」および「送信中...」のスピナー表示）が行われるようにUIを修正。さらに処理中は連打防止のため入力欄や操作ボタンが非活性化（`disabled`）されるように制御を強化しました。
  - ホーム画面（`page.tsx`）において、選択されたエクセルファイル名から自動抽出された担当者フォルダ内の関連ファイルをプレビュー表示する「画像ギャラリー（デザインデータ）」のJSX描画コードが、過去のコミットにより丸ごと削除されていた不具合を修正。最下部にギャラリーUIを完全に復元し、元々のデザイン画像読み取り機能が正しく動作・表示されるよう復帰させました。

## [2026-05-24]
### 追加 (Added)
- **スクリーンショット撮影用の自動プレースホルダーと画像用フォルダの事前準備（取扱説明書）**: ユーザー様が次回社内ネットワーク環境でスムーズにスクリーンショット画像を配置・埋め込めるよう、リポジトリルートに画像保存用の `docs_images` フォルダを物理的に作成（`.gitkeep` 配置）。さらに、実画像が存在しない状態でもマニュアル表示を美しく保ちつつ、撮影すべき画像ファイル名と保存先をビジュアルで誘導する「自動プレースホルダー表示（onerror時のJSによる自動差し替え制御）」を『営業日報システム_詳細取扱説明書.html』および『営業日報システム_詳細取扱説明書.md』の全12画面セクションに埋め込み完了。
- **簡単スタートガイド（初心者向け）各項目への画像プレースホルダー配置（取扱説明書）**: PC操作が苦手な一般ユーザー様向けに作成された「🔰 PCが苦手な方向け：かんたん1分スタートガイド」の各手順（計10ステップ）のすぐ下に、スクリーンショット画像のプレースホルダーを物理的に埋め込み。他セクションと同様に、実画像が存在しない状態でもマニュアル表示を美しく保ちつつ、撮影すべき画像ファイル名（`start_01_exe.png` 〜 `start_10_close_app.png`）をビジュアルで誘導する「自動プレースホルダー表示」を『営業日報システム_詳細取扱説明書.html』および『営業日報システム_詳細取扱説明書.md』に埋め込み完了。

### 修正 (Fixed)
- **売上分析のテーブルセル描画エラー（Reactコンポーネント内レンダリング違反）の解消**: 『SalesTable.tsx』にて、ソートアイコン表示用コンポーネント `SortIcon` およびヘッダーセル表示用コンポーネント `HeaderCell` が `SalesTable` のレンダリング関数内部で定義されていた問題（React hooksのフック・ステート破損リスクを引き起こすアンチパターン）を解消。コンポーネント定義をファイル内モジュールの最上位に移動させ、明確なプロパティ定義と明示的な戻り値の型アノテーション（`React.JSX.Element`）を適用したうえで、安全かつクリーンに描画されるよう修正しました。
- **新規・編集日報作成モーダル内でのESLint prefer-const警告の解消**: 『NewReportModal.tsx』および『EditReportModal.tsx』内の日報送信処理（`handleSubmit`）にて、再代入されないローカル変数 `finalFormData` が `let` で宣言されていた問題に対し、`const` 宣言へ変更し、リンター警告を解消しました。
- **各種ページ（売上分析、設定、日報一覧、月次活動サマリー）における副作用中の同期状態更新（cascading renders）エラーの解消**:
  * 『sales-analysis/page.tsx』にて、フィルター条件の切り替え時に `useEffect` 内で同期的に `currentPage` をリセットしていたのを、React推奨の「レンダリング中の状態調整パターン（レンダリングパス内でのState直接更新）」へ移行し、副作用（`useEffect`）の使用を完全に廃止しました。それに伴い、未使用の `useEffect` インポート警告も解消しました。
  * 『settings/page.tsx』、『reports/page.tsx』および『reports/summary/page.tsx』におけるマウント時のクライアント専用状態（オンラインステータス、PWA判定、マウント検知フラグ）の更新処理において、`useEffect` 内で同期的に `setState` を呼び出して再レンダリングを引き起こしていた問題に対し、`requestAnimationFrame` による非同期遅延実行を導入。初回のハイドレーションやレンダリングパスを邪魔することなく安全に動作するよう最適化しました。
  * 『settings/page.tsx』の PWA standalone 判定における TypeScript `as any` 型定義警告に対し、適切な Navigator 拡張キャスト（`as Navigator & { standalone?: boolean }`）を適用し、リンター警告（`no-explicit-any`）を解消しました。
  * すべての該当するページコンポーネント（`SalesAnalysisPage`, `SettingsPage`, `ReportsPage`）の関数定義に、ユーザーの全体ルールに即して明示的な戻り値の型アノテーション（`React.JSX.Element`）を適用しました。

### 変更 (Changed)
- **カレンダー機能の抽出対象仕様（訪問・社内業務のみ）の正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第4章「📅 ⑤ カレンダー」セクションにおいて、従来の「訪問や連絡」という不正確な表現を是正し、実際のプログラム側のフィルタリング仕様に即して「訪問（アポあり/なし/新規/クレーム）および社内業務（半日/1日）」のみが自動抽出・表示対象となること、および電話・メール連絡や量販店調査などは表示対象外となる旨を明記。
- **Excel手動編集時の管理番号不備に伴うデータ消失リスク等の警告追記（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の「3. 保存と自動採番・バックアップ」セクションにおいて、エクセル原本を直接開いて手動入力・修正する際の「管理番号（A列）」に関する重要注意（管理番号が未記入の場合に手動データが上書き消失する深刻なリスク、数値以外の文字入力や飛び値による自動連番狂い不具合、重複時のデータ上書きの競合エラー仕様など）を赤色のCaution（警告）バナーとして正確に追記。
- **新規日報入力における選択肢一覧の見出し表記置正および視認性劇的向上（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の「📝 ③ 新規日報入力」セクションにおいて、見出しに含まれていた「（誤りを完全排除）」という誇張表記を削除し、「2. エクセルの仕様に準拠した各プルダウンの選択肢一覧」に是正。さらに、各プルダウン選択肢一覧がカンマ区切りで極めて見にくかったため、**Markdown版では美しく余裕を持たせたパイプ（` | `）区切りのコードブロック形式**に変更し、**HTML版マニュアルでは個々の選択肢をCSSで美しく装飾されたマージン付きの「個別バッジ（タグ）」表示**へと置き換え、極めて高い視認性へとアップデート。
- **日報詳細における上長コメント返信および5段階承認チェック機能の解説追記（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第4章「📋 ② 日報一覧」セクションに、日報詳細ポップアップ（モーダル）内での「上長コメントの確認・返信機能（Excel W・X列）」および「5つの承認チェックボックス（既読、中野次長、上長、岡本常務、山澄常務: Excel Y〜AC列）」の具体的な仕様およびバックエンドAPI（`updateReportComment`, `updateReportApproval`）の仕組みを追記。さらに実務の運用に即し、**「一般営業担当者（課員）の場合」での指導の確認や返信、決裁状況の可視化メリット**と、**「指導・管理側（上長）や決裁・承認側（上長・役職者）の場合」でのダイレクト指導の入力、トグル操作による連続迅速決裁のメリット**の双方の立場からわかりやすく解説を再編・強化（「目線」という表現を完全に排し、「〜の場合」としてマニュアル向けに表現を厳格化）。
- **トラブルシューティング項目の拡充（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第6章「お助けトラブルシューティング（症状から探す）」に、同時編集競合エラー（楽観的ロック）と、新規得意先でのサジェスト候補欠落時の対応の2つの実用的なQ&Aを新しく追加。
- **Excel連携およびデータ管理の仕様の正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』の第5章「Excel連携およびデータ管理の仕様（一般ユーザー向け）」において、実際のバックエンド挙動に即した内容へと説明文をアップデート。バックアップの非同期実行（正常保存後）とフォルダ格納（`backup/` サブフォルダ）、およびセーフセーブ機能、楽観的ロック（同時編集時のコンフリクトエラー）、PermissionError時の画面警告表示について正確に追記。
- **設定画面（⑪ 設定）の詳細解説の正確化および同期反映（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』のL375-L381に記述されている第4章「⚙️ ⑪ 設定」の解説を大幅に拡張・是正。
  - 「📱 アプリステータス」でのネットワーク接続状態（オンライン/オフライン）のリアルタイム検知表示、およびPWA（Progressive Web App）のスタンドアロン動作自動判別表示の仕様を詳細化。
  - 「🔔 通知設定」でのブラウザのプッシュ通知許可制御（許可プロンプト提示と「許可済み」緑色ステータス切替）について正確に追記。
  - 「💰 売上データ管理 (CSVアップロード)」での社内システム（ABS）から抽出したCSVデータ（Shift-JIS / UTF-8）の取り込みおよび上書き確認ダイアログの安全設計などを追記。
  - 上記の極めて詳細かつ正確な解説構造を、『営業日報システム_詳細取扱説明書.html』の該当セクションにも完全に同期移植し、HTML版マニュアルの正確性と網羅性を是正。
- **分析・レポート機能群の詳細解説の正確化および同期反映（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』のL344-L354に記述されている第4章「📈 ⑩ 分析・レポート機能群」の解説を大幅に拡張・是正。
  - 「月次活動サマリー（`/reports/summary`）」の営業KPIカード（8種）やエリア別集計テーブル、アコーディオンによる直送先展開機能付き重点顧客活動一覧、ランキング、印刷・PDF出力連携の仕様を詳細化。
  - 「全メンバー活動分析・日報点数表（`/analytics/members`）」の得点分析（月200点目標、評価レート・達成率マトリクス）と活動集計（月度切り替え、並べ替え・ソート）の二面性機能、およびCSV/Excelダウンロード機能について正確に追記。
  - 「営業分析ダッシュボード（`/analytics`）」の4つの専用タブ（全体概要・商談分析・デザイン分析・重点顧客分析）ごとの集計チャート（混合分析グラフ、ヒートマップマトリクス、エリア活動PieChart、デザイン進捗ヒストグラム、期間フィルター等）の具体的な仕組みを解説。
  - 「売上分析（`/sales-analysis`）」の財務カード、得意先ランクや営業担当者、都道府県による動的連動フィルター、およびソート・ページネーション機能を完全に網羅。
  - 上記の極めて詳細かつ正確な解説構造を、『営業日報システム_詳細取扱説明書.html』の該当セクションにも完全に同期移植し、HTML版マニュアルの正確性と網羅性を是正。
- **特定業務検索機能（量販店調査・競合情報・クレーム）の詳細解説のHTML反映（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』のL303-L340に記述されている「⑦ 量販店調査検索」「⑧ 競合他社情報」「⑨ クレーム対応」セクションの機能解説（主な機能と活用方法、エリア/顧客別絞り込み、タイムライン表示、統計サマリーなど）を、『営業日報システム_詳細取扱説明書.html』にも完全に移植・同期し、HTML版マニュアルの網羅性を是正。
- **得意先一覧機能および詳細画面解説の不正確な記述の是正（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』における「得意先一覧」および「得意先詳細」セクションの解説において、実態と合致しない表現を是正。
  - 営業統計サマリーの記述を、実際の4つのカード（「総得意先数」「検索結果」「重点顧客」「総活動数」）に修正。
  - 「目標訪問頻度」「目標アプローチ頻度」の記述を、実態（現目標）に合わせ「目標（現目標）の確認」などに修正。
  - 詳細画面での「財務推移」の表記を、実際の「売上推移」に是正。
- **得意先詳細画面の解説詳細化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』における得意先一覧の「ピンポイント活動履歴と基本情報詳細」の解説を大幅に拡張。7つの統計サマリーカード、目標頻度表示バナー、および目的別の4つの専用タブ（タイムライン、デザイン案件、クレーム対応、過去3年の売上推移データ）の実装仕様を完全に網羅した詳細な解説にアップデート。
- **新規日報入力モーダルと一括入力の仕様不整合修正（プログラム）**: 『NewReportModal.tsx』において、得意先（訪問先名）が事前に入力されている場合に行動内容プルダウンから「社内（半日・１日）」「外出時間」を自動的に除外する動的フィルタリング機能を個別入力モーダルにも実装し、バッチ入力画面と仕様を統一。また、「外出時間」の満足度（ランク）プルダウンに欠落していた「0%」の選択肢を追加。
- **新規日報入力機能の解説正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の「新規日報入力」セクションにおいて、「量販店調査」や「社内業務/外出時間」選択時の動的フォームや個別入力画面・一括入力画面ごとの細かな仕様の違いを反映し、完全に事実と合致した高精度な内容に是正。
- **得意先の入力説明の更新（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』における得意先の入力説明を、「自動検索できます！」から「キーワード検索もできます」に更新。
- **エリア入力欄の説明文追記（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』におけるエリア（担当エリア）の入力説明にて、顧客リストから選択した場合には自動でエリアが記載される旨の説明を追記。
- **デザイン提案入力欄（既存選択時）の説明文追記（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』のデザイン提案入力欄において、ラジオボタンで「既存」を選択した場合にはプルダウンメニューから既存のデザインを選択する旨の説明を追記。
- **システム概要と特徴セクションの正確化・詳細化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第1章「システム概要と特徴」において、システムの実態に合わせた詳細な解説（新規保存時のセル書式自動コピー機能、メモリ＆ディスクキャッシュによる超高速マルチレイヤキャッシュ機構、および `backup/` フォルダへのタイムスタンプ付きの複製ファイル非同期保存による履歴バックアップ仕様）へと説明文および構成図をアップデート。
- **動作環境と事前準備セクションの詳細化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第2章「動作環境と事前準備」において、クライアントPCにおける共有フォルダに対する読み書きアクセス権限の必要性、使用するポート番号（8001）の確保、初回起動時のセキュリティ警告（SmartScreen）への対処準備など、運用開始にあたって重要な前提条件・確認事項を追加。
- **アクティブファイル切り替え仕様の正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の「アクティブファイルの切り替え」説明について、実態に合わないドラッグ＆ドロップ表記を完全に削除し、ヘッダーに配置された「選択リスト（プルダウン）」からの選択切り替え、および「読込」ボタンによるパソコン内ファイル選択でのインポート登録へと説明を実態に合わせて是正。
- **日報一覧機能の説明文詳細化・正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の第4章「📋 ② 日報一覧」において、実際の画面仕様（テーブルとタイムラインの2つの表示モード切替、昇順・降順ソート、50件ごとの高速ページネーション、クイックリフレッシュボタン、詳細モーダル内での前後日報移動・編集機能、新規一括入力への遷移ボタン）を完全に網羅した詳細な解説にアップデート。

## [2026-05-23]
### 変更 (Changed)
- **デザイン入力項目の正確化（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』のデザイン提案「新規」入力項目において、実際の画面仕様に合わせて「デザイン依頼No.」と「デザイン名」の説明を追記。また、デザイン依頼No.の括弧内にあった入力例や枝番号の注記を削除しシンプルな記述に変更。
- **上長コメントポップアップ機能の解説追記（取扱説明書）**: ホーム画面の機能一覧において、未読の上長コメントがある場合に「画面上部で赤色通知」され、その場でポップアップ（モーダル）を開いて内容の確認・編集ができることや、その場で返信・既読処理を行える機能についての説明を追記。
- **日報入力手順の画面順同期（取扱説明書）**: 初心者向けスタートガイドおよび機能解説における「行動内容の入力」について、変則的な「最後に入力する」というルール記述を廃止し、実際の画面入力フォームの並び順（上から下へ流れる自然な順序）に合わせて手順の説明順序を是正。また、得意先入力に伴う「自動絞り込み（動的フィルタリング）機能」の解説として説明をブラッシュアップ。
- **売上分析の仕様補足追記（取扱説明書）**: 『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の「💰 売上分析 (`/sales-analysis`)」の項目において、売上分析機能を有効にするには社内システム（ABS）から顧客別売上ランキング表を抽出し設定画面でアップロードする必要がある旨の注記を追記。
- **自動ブラウザ起動の解説追記（取扱説明書）**: 起動手順において、`DailyReportServer.exe` 実行後に自動でブラウザが立ち上がる仕様に合わせ、『営業日報システム_詳細取扱説明書.md』および『営業日報システム_詳細取扱説明書.html』の記述を「自動でブラウザが起動する」旨に更新。
- **表示表現の是正（取扱説明書）**: 画面上部の選択中エクセルファイル名表示について、誇張表現（「常に大きく表示」）を削除し、実態（ヘッダー部分に表示）に合わせた説明に変更。
- **デザイン進捗ステータスの正確化および各種機能の追記（取扱説明書）**: デザイン検索画面の「進捗ステータスバッジ」説明において、実際のプログラム内で定義されている進捗バッジ（新規、50％未満、80％未満、80％以上、出稿、保留、不採用（コンペ負け）、不採用（企画倒れ））に合わせた正確な文言に更新。また、デザインNo.に紐付いた関連デザイン画像の検索・プレビュー・ダウンロード機能、および展開した活動履歴から情報が自動入力された状態で日報を直接追加できる「ダイレクト日報追加機能」について新しく説明を追記（これに伴い、HTML版にも未記載だった各解説を追加）。
- **カレンダー機能の正確化（取扱説明書）**: カレンダー画面の解説において、一律表示の仕様（色分けは得意先毎ではなく、デザイン提案有の「★」表記）、クリックによる詳細ポップアップ機能、および印刷・PDFレポート出力機能について、プログラムの実装に即した正確な記述に更新（HTML版にも未記載だった解説を追加）。
- **得意先一覧機能の正確化（取扱説明書）**: 得意先一覧の解説において、得意先マスタ連携による目標訪問頻度の進捗度チェック、エリア・ランク・重点顧客による多角的フィルタリング、アコーディオンによる直送先の内訳表示、ピンポイント活動履歴詳細への遷移、営業統計サマリーなど、システムで実装されている強力な機能について正確かつ包括的に解説を追記。

### 追加 (Added)
- **営業日報システム詳細取扱説明書のHTML版を新規追加**: パッケージの一部として、ブラウザ上でダブルクリックするだけで即座に美しいWebドキュメントとして閲覧・活用できる『営業日報システム_詳細取扱説明書.html』を新規作成。レスポンシブな目次サイドバー、アコーディオンによる滑らかなQ&Aトラブルシューティング、洗練されたCSS/SVG構成図を完備した最高品質のプレミアムデザイン仕様。
- **初心者・PC苦手な方向けの取扱説明書改善**: ITリテラシーの低いユーザーでも安心してシステムを使用できるよう、以下の通り詳細取扱説明書を大幅に親切化・改善しました。
  - **📁 選択されているエクセルファイルの自動・常時確認（超重要ステップ）および上部ヘッダーによるどこからでも管理可能な仕様の追記**: 誤って他人のファイルに書き込んでしまうリスクを完全に排除するため、スタートガイドの手順1番目に「最初に自分のエクセルになっているか確認する」を最優先ステップとして新設。また、新規日報入力画面の機能紹介欄やホーム画面の説明欄にも「常時エクセルファイル名が表示され、視覚的に自動確認できる」親切な説明を追加。さらに、エクセルファイルの管理（切り替え・確認）は、特定の画面だけでなく**「どの画面からでも操作できるように画面上部のヘッダーに配置されている」という仕様上の説明を追記**。
  - **🔰「かんたん1分スタートガイド」の新設**: 専門用語を排除し、`DailyReportServer.exe` をダブルクリックしてブラウザに接続する起動手順と、各項目の入力方法・動的フォームの挙動まで手取り足取り教える「日報入力のやさしい手順」を新設。また、得意先名などの入力状況に応じて「行動内容」の不要な選択肢が自動で除外される仕様に合わせ、**「行動内容」を最後に入力する順序へと是正・解説を追記**。さらに、当日の日付が最初から自動入力されているため当日は操作不要であり、**後日入力の際のみ日付変更が必要である旨の解説も追記**。また、通常操作では選択不要な**「担当者（あなたの名前を選ぶ）」ステップを完全に削除し、登録フローをさらに簡素化**。さらに、得意先入力が**エクセルデータベースからの自動検索（キーワードサジェスト）である点や、リストにない場合の自由入力仕様について詳しく追記**。加えて、エクセル書き込みやバックアップ作成に伴う**保存時のタイムラグ（数秒程度）に関する注意喚起を追記し、連打や不正終了を防止**。
  - **📊 全プルダウン選択肢の完全網羅と是正**: スタートガイドおよび第4章「新規日報入力」において、実際のシステムコードで定義されている全てのプルダウン（行動内容13種類、滞在時間5種類、満足度5段階、デザイン種別5種類、デザイン進捗状況9種類）を完全に網羅・是正し、事実との不一致を排除。
  - **🤖「黒い画面」の安心解説の強化**: 起動時にコマンドプロンプトが立ち上がることへの不安（故障やウイルス感染の誤解）を払拭するため、目立つアラート形式で優しく解説。
  - **📁 一般ユーザーと管理者の情報分離**: `config.json` の書き換えや手動起動などの管理者向けセクションに「※一般の方は読まなくて大丈夫です」と明記し、一般ユーザーの心理的負荷を削減。
  - **🚑 症状から逆引きできる「お助けトラブルシューティング」**: 「エラーコード」ではなく、「画面が真っ白になった」「英語の警告が出る」など、目に見える症状から自分で解決できる対話式Q&Aを新設。
  - **☁️ オフライン同期の噛み砕き解説**: 専門用語を排し、「電波がない場所での一時保存と自動送信」として初心者でも直感的に理解できるように説明。
- **詳細取扱説明書の作成と破損修復**: システム全体の操作方法、動作環境、起動手順、年度切り替え仕様、およびトラブルシューティングを包括的に網羅した「営業日報システム 詳細取扱説明書」を新規追加し、ファイル破損（エンコーディングやセクション重複）の修復を行いました。
- **起動説明のEXE直接起動化**: 詳細取扱説明書内の起動手順の解説を、簡易配布パッケージの実態に合わせて `DailyReportServer.exe` を直接ダブルクリックして起動し、ブラウザでアクセスする説明に更新しました（これに伴い、VBSに関する補足説明を完全に削除し、起動方法をEXEに完全一本化しました）。

## [2026-05-15]
### 変更 (Changed)
- **年度設定の完全移行 (2025 → 2026)**: システム全体のデフォルト設定およびドキュメントを「2025年度」から「2026年度」に完全に統一しました。
  - `config.example.json`: デフォルトのExcelディレクトリを 2026年度 パスに更新。
  - `backend/main.py`: コード内のデフォルト値を 2026年度 に固定。
  - `使い方ガイド.md`: 説明文およびバージョン表記を 2026年度版 に更新。

### 修正 (Fixed)
- **配布パッケージの初期設定修正**: 配布用ZIP内の `config.json` が 2025年度 を指していた不具合を修正しました。

## [2026-05-14]
### 修正 (Fixed)
- **日報のソート順の修正**: 日付が 1 桁（例: 5/8）の場合に文字列比較で正しくソートされない問題を修正しました。内部的に `YY/MM/DD` 形式を正規化して比較するロジックを導入しました。
- **日付入力の自動正規化**: 日報入力時に `26/5/8` のように入力しても、保存時に `26/05/08` と自動的に 0 埋めされるように変更しました。
- **月次サマリーの比較ロジック修正**: 日付の比較（最終活動日の更新など）が文字列比較で行われていた箇所を、正規化された日付比較に変更しました。

### 変更 (Changed)
- **管理番号による副次ソート**: 日付が同じ日報については、管理番号の降順（新しい順）で表示されるようにしました。
- **月次サマリーのデフォルト表示改善**: 重点顧客活動テーブルにおいて、直送先の詳細をデフォルトで展開（オープン）した状態で表示するように変更しました。
- **ランキングの内訳表示追加**: 訪問/電話回数ランキングにおいて、得意先ごとの直送先別内訳を表示する機能を追加しました（「直接」のみの場合は表示を抑制し、視認性を向上）。
- **配布用パッケージの更新**: 上記修正を反映した `DailyReportSystem_2026_Simple.zip` を作成し、デスクトップに配置しました。
